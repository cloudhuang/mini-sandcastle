# 任务

请创建一个简单的 "Hello World" TypeScript 文件并提交。

1. 创建 src/hello.ts，内容：
   ```typescript
   export const hello = (name: string): string => `Hello, ${name}!`;
   ```
运行 git add 和 git commit

完成后输出：COMPLETE